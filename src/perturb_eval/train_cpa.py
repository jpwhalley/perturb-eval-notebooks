"""
train_cpa — CPA training wrapper.

Phase 3 engineering layer. This module documents the public-repo training
contract; full retraining is deliberately not packaged as the default
reproducibility path. The manuscript's 400 CPA prediction h5ads were
generated by author scripts described in TRAINING_PROVENANCE.md (see
repository root); the shipped artefacts in `data/predictions/severity_details/`
are the validated reference that downstream evaluation is built against.

Contract (any future implementation should satisfy):

    train_one_seed(cell_type: str, split: str, seed: int, n_epochs: int = 30,
                   output_dir: Path = Path('data/predictions')) -> Path
        Train CPA for a single (cell_type, split, seed) combination using the
        deterministic holdout specification from D03. Returns the path to the
        per-seed prediction AnnData. Approximately 2 minutes per call on CPU.
        Hyperparameters (matching the manuscript): 30 epochs, batch_size 128,
        Adam with the CPA TrainingPlan default lr (5e-4), input gene set = top 2000 HVGs
        plus perturbation target genes. CPU device (scvi-tools float64 logging
        is incompatible with MPS).

    train_panel(cell_types: list[str], splits: list[str], seeds: list[int],
                **kwargs) -> list[Path]
        Convenience wrapper that calls train_one_seed for each combination
        and returns the list of output paths.

To validate any prediction h5ad against the eval-layer contract:

    uv run python scripts/evaluate_severity_panel.py --validate-existing \\
        data/predictions/severity_details

This module requires the `cpa` dependency group:

    uv sync --group cpa

See P01_train_cpa.ipynb for the per-seed flow and the public-repo orchestration
shell; see TRAINING_PROVENANCE.md for the scripts that actually produced the
shipped 400 CPA prediction h5ads and the rationale for the layered design.
"""
