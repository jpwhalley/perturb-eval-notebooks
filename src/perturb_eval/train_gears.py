"""
train_gears — GEARS training wrapper.

Phase 3 engineering layer. This module documents the public-repo training
contract; full retraining is deliberately not packaged as the default
reproducibility path. The manuscript's 28 GEARS prediction h5ads (matched-n
cross-architecture audit, 4 conditions × 7 seeds) were generated by author
scripts described in TRAINING_PROVENANCE.md (see repository root); the
shipped artefacts in `data/predictions/severity_details/` are the validated
reference that downstream evaluation is built against.

Contract (any future implementation should satisfy):

    train_one_seed(cell_type: str, split: str, seed: int, n_epochs: int = 30,
                   output_dir: Path = Path('data/predictions')) -> Path
        Train GEARS for a single (cell_type, split, seed) combination using
        the deterministic holdout specification from D03. Used for the
        matched-n cross-architecture scope check; only seeds 1 through 7 are
        evaluated. Returns the path to the per-seed prediction AnnData.
        Hyperparameters: 30 epochs (or GEARS default), fixed training seed 1,
        same input gene set as CPA (top 2000 HVGs plus perturbation targets).
        MPS preferred, CPU fallback. Approximately 5-10 minutes per call.

    train_panel(cell_types: list[str], splits: list[str], seeds: list[int],
                **kwargs) -> list[Path]
        Convenience wrapper that calls train_one_seed for each combination
        and returns the list of output paths.

To validate any prediction h5ad against the eval-layer contract:

    uv run python scripts/evaluate_severity_panel.py --validate-existing \\
        data/predictions/severity_details

This module requires the `gears` dependency group:

    uv sync --group gears

See P02_train_gears.ipynb for the per-seed flow and the public-repo
orchestration shell; see TRAINING_PROVENANCE.md for the scripts that actually
produced the shipped 28 GEARS prediction h5ads and the rationale for the
layered design.
"""
